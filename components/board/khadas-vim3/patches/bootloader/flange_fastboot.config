# flange_fastboot.config — Khadas VIM3 fastboot gadget 与 eMMC boot0 路由
#
# mainline khadas-vim3_defconfig 默认只启用 DFU。U-Boot 的
# meson-g12-common-u-boot.dtsi 删除 Linux mmc aliases、保留控制器硬件顺序：
# sd_emmc_a=mmc0、sd_emmc_b=mmc1、sd_emmc_c(eMMC)=mmc2。bootloader 写
# hardware boot0，其余分区写 user-area GPT。
CONFIG_USB_FUNCTION_FASTBOOT=y
CONFIG_FASTBOOT=y
CONFIG_CMD_FASTBOOT=y
CONFIG_FASTBOOT_FLASH=y
CONFIG_FASTBOOT_FLASH_MMC_DEV=2
CONFIG_FASTBOOT_BUF_ADDR=0x6000000
CONFIG_FASTBOOT_BUF_SIZE=0x8000000
CONFIG_FASTBOOT_GPT_NAME="gpt"

CONFIG_SUPPORT_EMMC_BOOT=y
CONFIG_FASTBOOT_MMC_BOOT_SUPPORT=y
CONFIG_FASTBOOT_MMC_BOOT1_NAME="bootloader"
CONFIG_FASTBOOT_MMC_BOOT2_NAME="bootloader2"

# USB MaskROM 加载时自动进入 fastboot；eMMC/SD 冷启动继续正常 bootflow。
# partitions 由 U-Boot 的 oem format 按真实 eMMC 容量生成。
CONFIG_USE_PREBOOT=y
CONFIG_PREBOOT="setenv partitions 'name=boot,size=64MiB;name=rootfs,size=-;'; if test \"${boot_source}\" = \"usb\"; then echo \"flange: USB MaskROM 加载，自动进 fastboot...\"; fastboot usb 0; fi"
CONFIG_FASTBOOT_CMD_OEM_FORMAT=y
CONFIG_CMD_GPT=y
CONFIG_RANDOM_UUID=y
