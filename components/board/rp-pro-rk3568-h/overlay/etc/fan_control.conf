# fan_control.conf — rp-pro-rk3568-h 板级覆盖
# 覆盖 components/app/fan_control/conf/fan_control.conf 的默认值。

# 风扇接 GPIO3_B6：bank=3, B6=8+6=14 → 3*32+14 = 110
FAN_GPIO=110

# 高电平开风扇（已上板实测：写 1 转，写 0 停）
FAN_ACTIVE=high

# 温度阈值（毫摄氏度）：40°C 开，35°C 关，5°C 滞回
TEMP_ON=40000
TEMP_OFF=35000

INTERVAL=5
