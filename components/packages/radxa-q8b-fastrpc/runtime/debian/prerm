#!/bin/sh
set -xe

if [ -z "${DPKG_ROOT:-}" ] && [ "${1:-}" = "remove" ] \
        && [ -d /run/systemd/system ]; then
    systemctl stop \
        adsprpcd.service \
        adsprpcd_audiopd.service \
        cdsprpcd.service >/dev/null || true
fi
