menu "Tab5 AIO"

config TAB5_AUDIO_PANEL
    bool "音频 bring-up 屏上状态面板（调试设施，默认关闭）"
    default n
    help
      在待机画面下方画一块状态面板：三行文字 + 左右声道电平条。

      这块板默认没有任何可用串口（UART0 只在 M5-Bus 排针上，USB-Serial/JTAG 被
      TinyUSB 收走，CDC 与 UAC 音频在全速口上互斥），codec/I2S bring-up 阶段
      USB 侧又还什么都没有 —— 屏幕是那一段唯一看得见的输出。

      **这是调试设施，不是产品特性。** 打开后 PPA 多一个周期性提交者，
      占用 84 KB PSRAM（状态区 52 KB + 电平条 32 KB），电平条以 5 Hz 刷新
      约合 160 KB/s 的 PPA 搬运；出厂固件应保持关闭。

      ⚠️ **面板不会因为 host 上线而让位。** 它没有 gud_device_has_frame() 那道门
      （bring-up 期就是要在有 host 内容时也看得到状态），所以 host 送帧之后
      每次状态/电平刷新都会在主机画面的那一块上**打洞**，而 GUD 是脏矩形刷新的，
      被盖掉的区域主机不会自动重画 —— 洞会一直留在那里。开着它测显示是没有
      意义的，复合回归请务必关掉。

endmenu
