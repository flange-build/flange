dts: radxa-dragon-q6a i2c13 去掉 qcom,enable-gsi-dma（魅族 E3 屏触摸/背光）

魅族 E3 39pin 屏的触摸（sec_ts @0x48）与自带背光（sgm37604a @0x36）都挂在
i2c13（QUP1_SE5，FPC pin 26/27，原理图注释 "External touchscreen"）上。base
board dts 给 i2c13 声明了 `qcom,enable-gsi-dma`，强制 geni i2c 走 GPI/GSI DMA
模式。实板（linux-6.18.2）上该模式对这两个低速从机的小事务全部失败：

  geni_i2c a94000.i2c: DMA txn failed:3
  geni_i2c a94000.i2c: GPI transfer failed: -5   (-EIO)

后果：sec_ts 读 device id 全 0（应为 AC,6F,70）、read_information 全 0（Tx/Rx=0），
触摸 IC 实质不通信；sgm37604a 背光寄存器写入同样失败。

default 产物不挂任何 i2c13 从机（仅 status=okay），从未触发传输，所以此问题
在 default 上不暴露；只有启用 meizu-e3-panel 包、i2c13 上出现真实从机后才显现。

去掉 `qcom,enable-gsi-dma` 让 geni i2c 回退到 FIFO/SE-DMA 模式（高通低速传感器/
触摸总线的常规稳妥路径），触摸与背光事务恢复正常。i2c10（RTC st,m41t11）的
gsi-dma 工作正常、与本 bus 无关，保持不动。

本补丁对 default 产物无副作用（i2c13 无从机，FIFO/GSI 行为等价空操作）。

diff --git a/arch/arm64/boot/dts/qcom/qcs6490-radxa-dragon-q6a.dts b/arch/arm64/boot/dts/qcom/qcs6490-radxa-dragon-q6a.dts
index 518db4129..826d234a3 100644
--- a/arch/arm64/boot/dts/qcom/qcs6490-radxa-dragon-q6a.dts
+++ b/arch/arm64/boot/dts/qcom/qcs6490-radxa-dragon-q6a.dts
@@ -538,7 +538,6 @@ rtc: rtc@68 {

 /* External touchscreen */
 &i2c13 {
-	qcom,enable-gsi-dma;
 	status = "okay";
 };

