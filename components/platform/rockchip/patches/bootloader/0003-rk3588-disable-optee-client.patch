From: flange <flange@localhost>
Subject: [PATCH] configs/rk3588_defconfig: 关闭 OP-TEE 客户端

generic rk3588_defconfig 启用了 CONFIG_OPTEE_CLIENT 但没启用
CONFIG_SPL_OPTEE，导致 SPL 不打包 tee.bin 给 BL31，BL31 启动时
报 "No OPTEE provided by BL2"，u-boot proper 阶段又强制 client
检查（"optee check api revision fail: -1.0"）→ "ERROR: Please
RESET the board"，启动卡死。

flange 不使用 OP-TEE 受信应用，关闭 OPTEE_CLIENT 三件套即可让
u-boot proper 跳过 OP-TEE 检查，正常进入 Generic Distro Boot
读取 extlinux.conf。

仅影响 configs/rk3588_defconfig（其他 defconfig 不读此文件，
对 RK3566 板与 board-specific RK3588 defconfig 零影响）。

diff --git a/configs/rk3588_defconfig b/configs/rk3588_defconfig
--- a/configs/rk3588_defconfig
+++ b/configs/rk3588_defconfig
@@ -232,6 +232,3 @@
 CONFIG_AVB_LIBAVB_ATX=y
 CONFIG_AVB_LIBAVB_USER=y
 CONFIG_RK_AVB_LIBAVB_USER=y
-CONFIG_OPTEE_CLIENT=y
-CONFIG_OPTEE_V2=y
-CONFIG_OPTEE_ALWAYS_USE_SECURITY_PARTITION=y
