# U-Boot 环境变量存储位置（供 fw_setenv/fw_printenv 定位）。
# 与 components/platform/allwinnerh3/h3/config.py 的 bootloader.defconfig
# 中 CONFIG_ENV_IS_IN_MMC + 未覆盖的 sunxi 默认 ENV_OFFSET/ENV_SIZE 一致：
# sector 1920-2047（spl 分区结束到 boot 分区起始之间的天然空隙）。
#
# recoveryctl 的 env-only recovery 进入机制（`recoveryctl recovery
# --persistent` → `fw_setenv flange_boot_once recovery`）依赖此文件存在
# 且与实际 U-Boot 配置一致，否则 fw_setenv 会读写到错误的磁盘偏移。
#
# <device>       <offset>  <size>
/dev/mmcblk0      0xF0000   0x10000
