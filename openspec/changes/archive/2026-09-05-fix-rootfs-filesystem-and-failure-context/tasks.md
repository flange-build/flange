## 1. 存储与活树生命周期

- [x] 1.1 复现 sudo 解包错误并对照原生/宿主共享存储的 root 写入与 UID/GID 语义，记录证据（不超过 2 小时）
- [x] 1.2 实现 rootfs_storage 原生临时目录作用域，在公共编排覆盖 rootfs/recovery 全活树阶段（不超过 2 小时）
- [x] 1.3 保留持久化镜像/包清单/平台辅助路径，基础快照配方加入存储实现并验证恢复目的地（不超过 2 小时）
- [x] 1.4 清理前检查残留挂载，成功/失败/取消均安全收尾，不递归进入绑定挂载（不超过 2 小时）
- [x] 1.5 验证基础快照的扩展属性、文件 capability 与 ACL，确认并修复首次构建和恢复后的元数据差异（不超过 2 小时）

## 2. 主异常与清理诊断

- [x] 2.1 在进程失败时向异常实例绑定该命令的错误上下文，后续命令保持独立活动缓冲（不超过 2 小时）
- [x] 2.2 将 ChrootContext 初始化回滚与退出清理事务化，始终逆序尝试卸载，保留主异常并记录次级清理错误（不超过 2 小时）
- [x] 2.3 回归 APT 失败后成功清理、清理自身失败、已恢复异常不污染后续配置错误等场景（不超过 2 小时）

## 3. 验证与文档

- [x] 3.1 验证原生目录权限/所有权、TMPDIR 隔离、残留挂载防护，以及 rootfs/recovery/平台产物路径（不超过 2 小时）
- [x] 3.2 在真实 Docker 中执行最小 sudo/APT 与成像验证，按实际范围记录完整目标构建结果（不超过 2 小时）
- [x] 3.3 根据最终实现同步 ProjectSpec、入门/开发指南的存储条件、空间诊断与恢复流程（不超过 2 小时）
- [x] 3.4 完成定向/全量与严格规格校验，记录证据及未验证边界（不超过 2 小时）
- [x] 3.5 最终核对后同步主规格并归档变更（不超过 2 小时）
