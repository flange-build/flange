[
  "priv: tar --numeric-owner --xattrs --xattrs-include=* --acls -xf <TARBALL> -C <TMP>/rootfs",
  "priv: cp /usr/bin/qemu-aarch64-static <TMP>/rootfs/usr/bin",
  "priv: mount -t proc proc <TMP>/rootfs/proc",
  "priv: mount -t sysfs sysfs <TMP>/rootfs/sys",
  "priv: mount -o bind /dev <TMP>/rootfs/dev",
  "priv: mount -o bind /dev/pts <TMP>/rootfs/dev/pts",
  "priv: cp /etc/resolv.conf <TMP>/rootfs/etc/resolv.conf",
  "priv: mount -o bind <PROJECT>/.build/cache/apt <TMP>/rootfs/var/cache/apt/archives",
  "priv: chroot <TMP>/rootfs apt-get update",
  "priv: chroot <TMP>/rootfs apt-get install -y --no-install-recommends ca-certificates",
  "priv: chroot <TMP>/rootfs update-ca-certificates",
  "run: gpg --batch --yes --dearmor -o <TMP>/rootfs/etc/apt/keyrings/golden-ppa.gpg <APTKEY>",
  "priv: chroot <TMP>/rootfs apt-get update",
  "priv: chroot <TMP>/rootfs apt-get install -y --no-install-recommends systemd udev",
  "priv: chroot <TMP>/rootfs apt-get clean",
  "priv: umount <TMP>/rootfs/var/cache/apt/archives",
  "priv: umount <TMP>/rootfs/dev/pts",
  "priv: umount <TMP>/rootfs/dev",
  "priv: umount <TMP>/rootfs/sys",
  "priv: umount <TMP>/rootfs/proc",
  "priv: tar --numeric-owner --xattrs --xattrs-include=* --acls -I zstd -3 -T0 -cf <PROJECT>/.build/cache/rootfs-base/.base-<HASH>.tar.zst.<TEMP>.tmp -C <TMP>/rootfs .",
  "priv: tar tf <PROJECT>/.build/cache/rootfs-base/.base-<HASH>.tar.zst.<TEMP>.tmp",
  "priv: mount -t proc proc <TMP>/rootfs/proc",
  "priv: mount -t sysfs sysfs <TMP>/rootfs/sys",
  "priv: mount -o bind /dev <TMP>/rootfs/dev",
  "priv: mount -o bind /dev/pts <TMP>/rootfs/dev/pts",
  "priv: cp /etc/resolv.conf <TMP>/rootfs/etc/resolv.conf",
  "priv: chroot <TMP>/rootfs dpkg -i --force-confnew /tmp/flange-debs/golden_1.0_arm64.deb",
  "priv: umount <TMP>/rootfs/dev/pts",
  "priv: umount <TMP>/rootfs/dev",
  "priv: umount <TMP>/rootfs/sys",
  "priv: umount <TMP>/rootfs/proc",
  "priv: cp -a <PROJECT>/.build/target/golden-board/default/release/kernel/modules/lib/modules/. <TMP>/rootfs/lib/modules",
  "priv: cp -a <PROJECT>/components/rootfs/overlay/. <TMP>/rootfs",
  "priv: mount -t proc proc <TMP>/rootfs/proc",
  "priv: mount -t sysfs sysfs <TMP>/rootfs/sys",
  "priv: mount -o bind /dev <TMP>/rootfs/dev",
  "priv: mount -o bind /dev/pts <TMP>/rootfs/dev/pts",
  "priv: cp /etc/resolv.conf <TMP>/rootfs/etc/resolv.conf",
  "priv: chroot <TMP>/rootfs groupadd -r -f sudo",
  "priv: chroot <TMP>/rootfs groupadd -g 1000 arduino",
  "priv: chroot <TMP>/rootfs useradd -m -u 1000 -g arduino -s /bin/bash arduino",
  "priv: chroot <TMP>/rootfs usermod -aG sudo arduino",
  "priv: chroot <TMP>/rootfs chpasswd  <stdin:arduino:golden>",
  "priv: chroot <TMP>/rootfs chpasswd  <stdin:root:golden>",
  "verify: _verify_root_password",
  "priv: umount <TMP>/rootfs/dev/pts",
  "priv: umount <TMP>/rootfs/dev",
  "priv: umount <TMP>/rootfs/sys",
  "priv: umount <TMP>/rootfs/proc",
  "priv: mount -t proc proc <TMP>/rootfs/proc",
  "priv: mount -t sysfs sysfs <TMP>/rootfs/sys",
  "priv: mount -o bind /dev <TMP>/rootfs/dev",
  "priv: mount -o bind /dev/pts <TMP>/rootfs/dev/pts",
  "priv: cp /etc/resolv.conf <TMP>/rootfs/etc/resolv.conf",
  "priv: chroot <TMP>/rootfs /bin/sh -c dpkg-query -W -f='${binary:Package}\t${Version}\n' | sort > /etc/flange/packages.manifest",
  "priv: umount <TMP>/rootfs/dev/pts",
  "priv: umount <TMP>/rootfs/dev",
  "priv: umount <TMP>/rootfs/sys",
  "priv: umount <TMP>/rootfs/proc",
  "priv: cp <PROJECT>/.build/target/golden-board/default/release/kernel/Image <TMP>/rootfs/boot/vmlinuz-6.1.0",
  "priv: cp <PROJECT>/.build/target/golden-board/default/release/kernel/config <TMP>/rootfs/boot/config-6.1.0",
  "priv: chown -R 1000:1000 <TMP>/rootfs/home/arduino",
  "priv: mount -t proc proc <TMP>/rootfs/proc",
  "priv: mount -t sysfs sysfs <TMP>/rootfs/sys",
  "priv: mount -o bind /dev <TMP>/rootfs/dev",
  "priv: mount -o bind /dev/pts <TMP>/rootfs/dev/pts",
  "priv: cp /etc/resolv.conf <TMP>/rootfs/etc/resolv.conf",
  "priv: chroot <TMP>/rootfs ldconfig",
  "priv: chroot <TMP>/rootfs /usr/lib/flange/unoq/verify-runtime --build",
  "priv: chroot <TMP>/rootfs /usr/lib/flange/unoq/verify-usb",
  "priv: chroot <TMP>/rootfs depmod -a 6.1.0",
  "priv: chroot <TMP>/rootfs update-initramfs -c -k 6.1.0",
  "priv: chroot <TMP>/rootfs lsinitramfs /boot/initrd.img-6.1.0",
  "priv: chroot <TMP>/rootfs systemctl enable arduino-router.service arduino-app-cli.service flange-unoq-data.service flange-unoq-grow-userdata.service rmtfs.service tqftpserv.service qbootctl.service adbd.service flange-unoq-audio.service arduino-avahi-serial.service arduino-cloud-connector.service arduino-router-serial.path",
  "priv: umount <TMP>/rootfs/dev/pts",
  "priv: umount <TMP>/rootfs/dev",
  "priv: umount <TMP>/rootfs/sys",
  "priv: umount <TMP>/rootfs/proc",
  "priv: chown -R 1000:1000 <TMP>/rootfs/home/arduino",
  "priv: cp <TMP>/rootfs/boot/initrd.img-6.1.0 <TMP>/initrd.img",
  "priv: cp <TMP>/rootfs/usr/lib/systemd/boot/efi/systemd-bootaa64.efi <TMP>/bootaa64.efi",
  "run: du -sm <TMP>/rootfs/home/arduino",
  "run: truncate -s 3072M <TMP>/userdata.img",
  "run: mke2fs -t ext4 -L userdata -U 66f6d9a8-6ce3-46f6-b679-a1a1d991ba19 -E root_owner=1000:1000 -F -q -d <TMP>/rootfs/home/arduino <TMP>/userdata.img",
  "priv: find <TMP>/rootfs/home/arduino -mindepth 1 -delete",
  "run: du -sm <TMP>/rootfs",
  "run: truncate -s 2048M <TMP>/rootfs.img",
  "run: mke2fs -t ext4 -L rootfs -F -q -d <TMP>/rootfs <TMP>/rootfs.img"
]
